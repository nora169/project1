from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='The Hangman program randomly selects a secret word from a list of secret words.',
    author='Me',
    license='',
)
